import React from 'react';
import ChannelArchive from '../../components/channel/ChannelArchive';

const ChannelArchiveContainer = (props) => {
  return <ChannelArchive />;
};

export default ChannelArchiveContainer;
